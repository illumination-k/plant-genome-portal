// Minimal test profile. Run with:
//   nextflow run pipelines/atacseq -profile docker,test
//
// Override these paths with your own fixture before running.

params {
    input        = "${projectDir}/samplesheets/samplesheet.example.csv"
    genome_fasta = null
    macs_gsize   = 2.0e4
}

process {
    cpus   = 2
    memory = 6.GB
}
