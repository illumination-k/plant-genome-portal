// Minimal test profile. Run with:
//   nextflow run pipelines/interproscan -profile docker,test
//
// Override the sample manifest with real FASTA paths before running.

params {
    genomes = "${projectDir}/samplesheets/genomes.example.csv"
}

process {
    cpus   = 2
    memory = 6.GB
}
